# Reference

Describes Review Bridge v0.15.1.

Generated by `node scripts/render-reference.mjs` from the tools `src/server.mjs`
registers and the error codes `src/` raises. Do not edit it by hand.

## Author tools

The server started with `--role author`. The summary is the first sentence of each tool's description.

| Tool | Summary | Required inputs |
| --- | --- | --- |
| `abandon_workflow_action` | Drop an executing action that the publication has since observed in a state that settles it. | `workflow_id`, `expected_revision`, `action_id` |
| `acknowledge_change_size_warning` | Record the explicit split decision a crossed change-size warning demands before the workflow may prepare its next review round: continue with a stated reason, or split with the intended cut. | `workflow_id`, `expected_revision`, `decision`, `rationale`, `operator_label` |
| `acknowledge_codex_review_ambiguity` | Record a direct human NO_FURTHER_RESULTS_EXPECTED decision for the exact complete request and ambiguous-result closure sets. | `review_id`, `expected_revision`, `head_sha`, `request_refs`, `ambiguous_results`, `acknowledgement`, `operator_label`, `rationale` |
| `advance_local_workflow` | Re-read the bound local-review ledger, refresh each newly captured snapshot's change size, and advance the matching two-round CODEX_TASK state, continue uncontested new findings, or pause when required. | `workflow_id`, `expected_revision` |
| `advance_remote_workflow` | Re-read the bound publication's autonomous projection and take the single transition it implies: a repair phase for a new head, a fail-closed pause, the pre-ready stop, or continued waiting. | `workflow_id`, `expected_revision` |
| `append_review_erratum` | Append an author correction for a claim about the world that went stale during the review. | `review_id`, `text` |
| `authorize_remote_publication` | Create an immutable review ID that explicitly skips local review and binds a clean local repository, base, head, operator, and rationale before GitHub-only publication review. | `repository_path`, `base_sha`, `head_sha`, `acknowledgement`, `operator_label`, `rationale` |
| `bind_workflow_publication` | Bind the version-3 publication started for the current gated head and enter the remote wait, recording only which publication revision the workflow awaits. | `workflow_id`, `expected_revision`, `review_id` |
| `bind_workflow_review` | Bind one new CODEX_TASK review only when its repository, requirement, scope, base, and head equal the workflow, report warning-threshold headroom, and pause before dispatch when its immutable change size exceeds the workflow budget. | `workflow_id`, `expected_revision`, `review_id` |
| `cancel_autonomous_workflow` | Explicitly stop future workflow writes while retaining branches, reviews, audit evidence, and ownership claims. | `workflow_id`, `expected_revision`, `operator_label`, `rationale` |
| `complete_workflow_action` | Complete a uniquely observed action and advance the workflow without performing another provider write. | `workflow_id`, `expected_revision`, `action_id` |
| `export_human_arbitration` | Read a HUMAN_REQUIRED review at an exact state version and return structured canonical ledger data plus deterministic copyable Markdown without changing review state. | `review_id`, `expected_state_version` |
| `extend_change_size_budget` | Explicitly raise an exceeded autonomous change-size budget without resuming the workflow or changing its authorization digest. | `workflow_id`, `expected_revision`, `new_budget`, `operator_label`, `rationale` |
| `extend_local_cycle_budget` | Explicitly raise an exhausted autonomous local-cycle budget without resuming the workflow or changing its authorization digest. | `workflow_id`, `expected_revision`, `new_budget`, `operator_label`, `rationale` |
| `extend_remote_cycle_budget` | Explicitly raise an exhausted autonomous remote-cycle budget without resuming the workflow or changing its authorization digest. | `workflow_id`, `expected_revision`, `new_budget`, `operator_label`, `rationale` |
| `finalize_local_gate` | Verify the working tree still matches the CLEAN snapshot and write a local gate attestation. | `review_id` |
| `finalize_publication_gate` | Recompute a fresh MERGE_READY ledger and issue an audited, expiring publication gate without changing the ledger revision. | `review_id`, `expected_revision` |
| `get_autonomous_pre_ready` | Evaluate every publication invariant in its normal fail-closed order with the draft flag alone ignored, and return the exact normalized blocker set behind the result. | `review_id` |
| `get_autonomous_terminal` | Evaluate the post-ready observation through the terminal projection: require publication MERGE_READY, revalidate the workflow binding and both authorization digests, and replay every automatic-resolution record and lifecycle chain against the same observation, returning the fail-closed blocker set that prevents a terminal record. | `review_id` |
| `get_autonomous_workflow` | Read the complete workflow ledger and reconcile one committed action-audit event when recovery requires it. | `workflow_id` |
| `get_autonomous_workflow_summary` | Read the exact revision, phase, next action, bound head and review, recoverable active-action dispatch, and pause reason. | `workflow_id` |
| `get_invalidated_resolution_plan` | Derive the one workflow-owned automatic resolution the current complete observation invalidates, including its exact old/new watermarks, follow-up comments, and safe or unsafe reason. | `review_id` |
| `get_publication` | Read the current publication revision, derived state, immutable target, and recorded evidence without accessing GitHub. | `review_id` |
| `get_publication_summary` | Read the current revision, blocking reason, next action, gate state, remaining seconds on a finalized gate, and exact ambiguity acknowledgement sets without returning the full publication ledger or accessing GitHub. | `review_id` |
| `get_review` | Read findings, author resolutions, decisions, errata, and state. | `review_id` |
| `get_review_summary` | Read current state, next action, snapshot identity, and compact finding counts without returning the full review ledger. | `review_id` |
| `get_thread_resolution_plan` | Per-thread eligibility verdicts for the publication's recorded observation: refusal reasons for ineligible threads; addressed-by commits, comment IDs, exact comment watermark, and eligibility digest for eligible ones. | `review_id` |
| `list_autonomous_workflows` | List compact autonomous workflow states, each with the required_inputs of its own next action, without advancing them. |  |
| `list_reviews` | List Review Bridge tasks and their current states. |  |
| `mark_workflow_action_executing` | Durably record EXECUTING immediately before the planned external write; a push additionally requires the pinned URL resolved to the authorized repository ID, a thread resolution requires the immediately preceding thread pre-read (thread ID, resolved flag, and -- while unresolved -- the exact comment watermark), a compensating unresolve requires the exact authorized pull request, thread, invalidated watermark, and resolved flag, and a mark-ready requires the immediately preceding pull-request pre-read (repository, number, both branches, head SHA, and draft flag). | `workflow_id`, `expected_revision`, `action_id` |
| `pause_autonomous_workflow` | Fail closed when task orchestration, action reconciliation, authorization, permission, or progress evidence is unavailable, or when a required check, a base merge, or a history rewrite needs a judgement the server cannot derive. | `workflow_id`, `expected_revision`, `reason_code`, `blocked_action`, `evidence` |
| `plan_codex_task_dispatch` | Persist a single CREATE_CODEX_REVIEWER_TASK intent and return its exact opaque marker, task title, and prompt. | `workflow_id`, `expected_revision`, `review_id` |
| `plan_draft_pull_request` | Persist a single CREATE_DRAFT_PULL_REQUEST intent pinned to the authenticated creator and return the exact body marker that binds the created pull request. | `workflow_id`, `expected_revision`, `creator_actor_id`, `creator_actor_type` |
| `plan_mark_pull_request_ready` | Persist the MARK_PR_READY intent for the workflow-owned pull request. | `workflow_id`, `expected_revision` |
| `plan_return_to_draft` | Persist the RETURN_PR_TO_DRAFT intent for the workflow-owned pull request. | `workflow_id`, `expected_revision` |
| `plan_thread_reply` | Persist a single REPLY_TO_CODEX_THREAD intent for an eligible finding thread and return the server-composed reply body naming the addressed-by commits with its correlation marker. | `workflow_id`, `expected_revision`, `thread_id`, `actor_id`, `actor_type` |
| `plan_thread_resolution` | Persist a single RESOLVE_REVIEW_THREAD intent for a replied, still-eligible thread, binding the reply-inclusive comment watermark and eligibility digest the resolution must hold. | `workflow_id`, `expected_revision`, `thread_id` |
| `plan_thread_unresolve` | Persist an UNRESOLVE_REVIEW_THREAD intent only for the exact workflow-owned resolution the publication server currently reports invalid. | `workflow_id`, `expected_revision`, `thread_id` |
| `plan_workflow_push` | Persist a single PUSH_TOPIC_BRANCH intent after verifying the clean checked-out HEAD still equals the gated workflow head. | `workflow_id`, `expected_revision` |
| `prepare_rereview` | Capture the updated code as round two after all findings have author responses. | `review_id` |
| `prepare_review` | Capture an immutable Git snapshot, requirement, implementation scope, patch, added-plus-deleted line measurement, warning-threshold headroom, test context, and explicit reviewer provider. | `repository_path`, `base_ref`, `requirement`, `implementation_scope`, `reviewer_provider` |
| `record_automatic_resolution` | Store the server-owned proof that this workflow performed the thread's unresolved-to-resolved transition. | `review_id`, `expected_revision`, `workflow_id`, `action_id` |
| `record_automatic_unresolve` | Append the server-owned INVALIDATED and UNRESOLVED_FOR_REPAIR lifecycle events from the observed unresolve action, then clear the stale observation. | `review_id`, `expected_revision`, `workflow_id`, `action_id` |
| `record_codex_review_request` | Immediately bind the exact summary-provided Codex review body, its request ID when present, and the posted issue-comment response to the freshly verified pull-request head, then clear any pre-post snapshot. | `review_id`, `expected_revision`, `comment_id`, `url`, `created_at`, `requested_head_sha` |
| `record_codex_task_observation` | Reconcile exactly one task whose title and prompt equal the server-issued dispatch payload. | `workflow_id`, `expected_revision`, `action_id`, `matching_task_ids`, `task_id`, `title`, `prompt` |
| `record_draft_pull_request_observation` | Reconcile exactly one marker-bound draft pull request whose repository, branches, head, and creator match the authorized target. | `workflow_id`, `expected_revision`, `action_id`, `matching_pr_numbers`, `pr_number`, `repository_id`, `head_repository_id`, `base_branch`, `head_branch`, `head_sha`, `draft`, `body_marker`, `creator_actor_id`, `creator_actor_type`, `url` |
| `record_github_snapshot` | Validate and persist one normalized GitHub observation covering pull-request identity, policy and checks, Codex evidence, and review threads. | `review_id`, `expected_revision` |
| `record_mark_ready_observation` | Reconcile the pull request after the mark-ready call: the same repository, number, branches, and head, now out of draft. | `workflow_id`, `expected_revision`, `action_id`, `outcome`, `repository_id`, `pr_number`, `base_branch`, `head_branch`, `head_sha`, `is_draft` |
| `record_push_observation` | Reconcile the push from a fresh provider read: the observed remote URL, resolved numeric repository ID, and ref head must prove the authorized repository and the exact pushed workflow head. | `workflow_id`, `expected_revision`, `action_id`, `remote_ref_sha`, `remote_repository_id`, `remote_url` |
| `record_return_to_draft_observation` | Reconcile the pull request after the return-to-draft call: the same repository, number, branches, and head, now a draft. | `workflow_id`, `expected_revision`, `action_id`, `outcome`, `repository_id`, `pr_number`, `base_branch`, `head_branch`, `is_draft` |
| `record_thread_reply_observation` | Reconcile exactly one marker comment in the exact thread by the pinned actor whose body equals the server-issued reply payload. | `workflow_id`, `expected_revision`, `action_id`, `matching_comment_ids`, `comment_id`, `thread_id`, `actor_id`, `actor_type`, `body` |
| `record_thread_resolution_observation` | Record the post-read after the resolve mutation: RESOLVED requires the unchanged watermark and resolvedBy equal to the action's own actor; OBSERVED_PRE_RESOLVED records a thread found already resolved, with no ownership. | `workflow_id`, `expected_revision`, `action_id`, `outcome`, `thread_id`, `is_resolved` |
| `record_thread_unresolve_observation` | Record the exact post-read for a compensating unresolve; an already-unresolved pre-read reconciles without issuing another mutation. | `workflow_id`, `expected_revision`, `action_id`, `outcome`, `repository_id`, `pr_number`, `thread_id`, `is_resolved`, `thread_watermark` |
| `record_workflow_head` | Verify the authorized topic branch is clean at an exact descendant commit and append the head attempt. | `workflow_id`, `expected_revision`, `head_sha` |
| `release_workflow_claims` | Release every active claim only after exact caller-supplied reconciliation proves each branch and head ref absent and each bound pull request closed. | `workflow_id`, `expected_revision`, `operator_label`, `rationale`, `reconciled_claims` |
| `render_review_report` | Render a human-readable Markdown report of a review from its ledger and, when present, its publication ledger -- or, for a REMOTE_ONLY publication with no review ledger, from the publication and its authorization alone -- and write it to reviews/<review_id>/report-r<state_version>[-p<revision>-s<summary digest>]-f<renderer format>.md (report-p<revision>-s<summary digest>-f<renderer format>.md when remote-only) beside them; the summary digest covers the publication summary fields the report prints, so a gate appearing or evidence expiring writes a new report, and the renderer format version means an upgrade that changes the Markdown writes a new report beside the old one rather than colliding with it. | `review_id` |
| `resume_autonomous_workflow` | Resume a transiently paused workflow at its audited prior phase after the blocking condition is cleared. | `workflow_id`, `expected_revision`, `operator_label`, `rationale` |
| `start_autonomous_workflow` | Create an opt-in workflow ledger at an immutable clean base, bind the exact capability set, and atomically claim the local branch and GitHub head ref. | `repository_path`, `base_ref`, `base_sha`, `requirement`, `implementation_scope`, `topic_branch`, `operator_label`, `capabilities`, `publication_target` |
| `start_publication` | Bind a local review gate or explicit remote-only authorization to one pull request, pinned Codex Bot actor, trigger policy, and fresh complete preexisting Codex baseline. | `review_id`, `repository_id`, `owner`, `repo`, `pr_number`, `base_branch`, `head_branch`, `codex_actor_id`, `codex_actor_type`, `codex_actor_login`, `codex_trigger_mode`, `codex_review_baseline` |
| `submit_resolutions` | Answer every open finding as fixed, rejected with evidence, or human_required. | `review_id`, `resolutions` |
| `verify_publication_gate` | Immediately before merge, cross-check the committed gate, current ledger revision and freshness, then durably audit the verdict. | `review_id` |
| `wait_for_review_state` | Wait 25 seconds by default, configurable up to 30 seconds, for the review's state machine to move past a known state_version. | `review_id`, `known_state_version` |

## Reviewer tools

The server started with `--role reviewer`. The summary is the first sentence of each tool's description.

| Tool | Summary | Required inputs |
| --- | --- | --- |
| `list_pending_reviews` | List review tasks currently waiting for initial review or rereview. |  |
| `open_review` | Read the requirement, implementation scope, changed files, prior findings, author responses, errata, and any carried scope hints. | `review_id` |
| `read_review_artifact` | Read a chunk of a successor delta/proof or the full patch/snapshot manifest. | `review_id`, `round`, `artifact` |
| `read_snapshot_file` | Read a repository file exactly as it existed in the selected review round. | `review_id`, `round`, `path` |
| `search_snapshot` | Search text across the reviewed Git tree and captured working-tree overlays. | `review_id`, `round`, `pattern` |
| `submit_rereview` | Decide every prior finding and report any new findings. | `review_id`, `decisions`, `new_findings` |
| `submit_review` | Submit structured initial findings. | `review_id`, `findings` |

## Error codes

A failed tool call returns one of these in its `code` field.
A code is listed when `src/` passes it as a literal where a function or constructor declares a `code` parameter,
or returns it from a `*ErrorCode` lookup. A code chosen at run time from a variable is not seen.

| Code | Raised in |
| --- | --- |
| `ACKNOWLEDGEMENT_SET_MISMATCH` | `src/publication.mjs` |
| `AUDIT_CORRUPT` | `src/publication.mjs` |
| `AUDIT_EVENT_TOO_LARGE` | `src/publication.mjs` |
| `AUDIT_STATE_INVALID` | `src/publication.mjs` |
| `AUDIT_WRITE_FAILED` | `src/publication.mjs` |
| `EVIDENCE_FUTURE` | `src/publication.mjs` |
| `EVIDENCE_NOT_ATOMIC` | `src/publication.mjs` |
| `EVIDENCE_STALE` | `src/publication.mjs` |
| `INVALID_INPUT` | `src/publication.mjs` |
| `INVALID_REVIEW_ID` | `src/report.mjs` |
| `LEDGER_UNREADABLE` | `src/report.mjs` |
| `LOCAL_GATE_INVALID` | `src/publication.mjs`, `src/report.mjs` |
| `LOCAL_GATE_MISSING` | `src/report.mjs` |
| `LOCAL_HEAD_MISMATCH` | `src/publication.mjs` |
| `LOCAL_REPOSITORY_ERROR` | `src/publication.mjs` |
| `LOCAL_WORKTREE_DIRTY` | `src/publication.mjs` |
| `LOCK_CLEANUP_FAILED` | `src/storage.mjs` |
| `LOCK_COORDINATOR_BUSY` | `src/storage.mjs` |
| `LOCK_COORDINATOR_INVALID` | `src/storage.mjs` |
| `LOCK_HELPER_FAILED` | `src/storage.mjs` |
| `LOCK_HELPER_KILL_FAILED` | `src/storage.mjs` |
| `LOCK_HELPER_TIMEOUT` | `src/storage.mjs` |
| `LOCK_OWNERSHIP_LOST` | `src/storage.mjs` |
| `LOCK_OWNER_UNKNOWN` | `src/storage.mjs` |
| `LOCK_RECORD_INVALID` | `src/storage.mjs` |
| `LOCK_RUNTIME_UNAVAILABLE` | `src/storage.mjs` |
| `OBSERVATION_FILE_MALFORMED` | `src/publication.mjs` |
| `OBSERVATION_FILE_TOO_LARGE` | `src/publication.mjs` |
| `OBSERVATION_FILE_UNREADABLE` | `src/publication.mjs` |
| `PUBLICATION_ALREADY_STARTED` | `src/publication.mjs` |
| `PUBLICATION_AUTHORIZATION_INVALID` | `src/publication.mjs` |
| `PUBLICATION_BUSY` | `src/storage.mjs` |
| `PUBLICATION_GATE_INVALID` | `src/publication.mjs` |
| `PUBLICATION_HISTORY_CONFLICT` | `src/publication.mjs` |
| `PUBLICATION_LIMIT_EXCEEDED` | `src/publication.mjs` |
| `PUBLICATION_MOVED_DURING_RENDER` | `src/report.mjs` |
| `PUBLICATION_NOT_AUTONOMOUS` | `src/publication.mjs` |
| `PUBLICATION_NOT_FOUND` | `src/publication.mjs` |
| `PUBLICATION_NOT_READY` | `src/publication.mjs` |
| `PUBLICATION_REVISION_CONFLICT` | `src/publication.mjs` |
| `PUBLICATION_STORE_INVALID` | `src/publication.mjs` |
| `PUBLICATION_SUPERSEDED` | `src/publication.mjs` |
| `PUBLICATION_TERMINAL` | `src/publication.mjs` |
| `PUBLICATION_THREAD_EVIDENCE_MISSING` | `src/publication.mjs` |
| `REMOTE_AUTHORIZATION_INVALID` | `src/publication.mjs` |
| `REPORT_FILE_MISMATCH` | `src/report.mjs` |
| `REPOSITORY_OBJECT_FORMAT_UNPUBLISHABLE` | `src/publication.mjs` |
| `REVIEW_BUSY` | `src/storage.mjs` |
| `REVIEW_LEDGER_INVALID` | `src/report.mjs` |
| `REVIEW_LEDGER_MISSING` | `src/report.mjs` |
| `REVIEW_MOVED_DURING_RENDER` | `src/report.mjs` |
| `REVIEW_NOT_FOUND` | `src/report.mjs` |
| `STORE_FILE_TOO_LARGE` | `src/storage.mjs` |
| `STORE_FILE_TYPE_INVALID` | `src/publication.mjs`, `src/storage.mjs` |
| `STORE_MODE_MISMATCH` | `src/publication.mjs`, `src/storage.mjs` |
| `STORE_NOFOLLOW_UNAVAILABLE` | `src/storage.mjs` |
| `STORE_WRITE_INDETERMINATE` | `src/storage.mjs` |
| `THREAD_RESOLUTION_NOT_INVALIDATED` | `src/workflow.mjs` |
| `UNSUPPORTED_PUBLICATION_VERSION` | `src/publication.mjs` |
| `WORKFLOW_ACTION_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_ACTION_KIND_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_ACTION_NOT_ABANDONABLE` | `src/workflow.mjs` |
| `WORKFLOW_ACTION_STATE_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_AUDIT_CORRUPT` | `src/workflow.mjs` |
| `WORKFLOW_AUDIT_EVENT_TOO_LARGE` | `src/workflow.mjs` |
| `WORKFLOW_AUDIT_LOG_FULL` | `src/workflow.mjs` |
| `WORKFLOW_AUTHORIZATION_INVALID` | `src/workflow-binding.mjs`, `src/workflow.mjs` |
| `WORKFLOW_AUTHORIZATION_MISMATCH` | `src/publication.mjs` |
| `WORKFLOW_BASE_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_BRANCH_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_BUSY` | `src/storage.mjs` |
| `WORKFLOW_CANCELLATION_RESERVE_EXHAUSTED` | `src/workflow.mjs` |
| `WORKFLOW_CANCELLED` | `src/publication.mjs` |
| `WORKFLOW_CAPABILITY_REQUIRED` | `src/workflow.mjs` |
| `WORKFLOW_CHANGE_SIZE_BUDGET_EXCEEDED` | `src/workflow.mjs` |
| `WORKFLOW_CHANGE_SIZE_SPLIT_UNEXECUTED` | `src/workflow.mjs` |
| `WORKFLOW_CHANGE_SIZE_WARNING_UNACKNOWLEDGED` | `src/workflow.mjs` |
| `WORKFLOW_CLAIMS_ALREADY_RELEASED` | `src/workflow.mjs` |
| `WORKFLOW_CLAIMS_BUSY` | `src/storage.mjs` |
| `WORKFLOW_CLAIMS_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_FINDINGS_UNIDENTIFIED` | `src/workflow.mjs` |
| `WORKFLOW_HEAD_MISMATCH` | `src/publication.mjs`, `src/workflow.mjs` |
| `WORKFLOW_HEAD_NOT_DESCENDANT` | `src/workflow.mjs` |
| `WORKFLOW_LOCAL_GATE_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_NOT_ACTIVE` | `src/publication.mjs`, `src/workflow.mjs` |
| `WORKFLOW_NOT_FOUND` | `src/workflow-binding.mjs`, `src/workflow.mjs` |
| `WORKFLOW_NO_PROGRESS` | `src/workflow.mjs` |
| `WORKFLOW_OWNERSHIP_CONFLICT` | `src/workflow.mjs` |
| `WORKFLOW_PHASE_INVALID` | `src/publication.mjs`, `src/workflow.mjs` |
| `WORKFLOW_PUBLICATION_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_PUBLICATION_NOT_READY` | `src/workflow.mjs` |
| `WORKFLOW_PULL_REQUEST_AMBIGUOUS` | `src/workflow.mjs` |
| `WORKFLOW_PULL_REQUEST_EXPOSED` | `src/workflow.mjs` |
| `WORKFLOW_RELEASE_EVIDENCE_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_REPOSITORY_DRIFT` | `src/workflow.mjs` |
| `WORKFLOW_RESOLUTION_ACTION_MISSING` | `src/publication.mjs` |
| `WORKFLOW_RESOLUTION_RECORD_MISSING` | `src/workflow.mjs` |
| `WORKFLOW_RESUME_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_REVIEWER_TASK_REQUIRED` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_BINDING_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_DIRTY` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_HEAD_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_OWNERSHIP_CONFLICT` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_RAN_AHEAD` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_STATE_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_REVIEW_TRANSITION_INVALID` | `src/workflow.mjs` |
| `WORKFLOW_REVISION_CONFLICT` | `src/publication.mjs`, `src/workflow.mjs` |
| `WORKFLOW_SCHEMA_UNSUPPORTED` | `src/workflow-binding.mjs`, `src/workflow.mjs` |
| `WORKFLOW_STATE_INVALID` | `src/workflow-binding.mjs`, `src/workflow.mjs` |
| `WORKFLOW_STATE_TOO_LARGE` | `src/workflow.mjs` |
| `WORKFLOW_TASK_AMBIGUOUS` | `src/workflow.mjs` |
| `WORKFLOW_TASK_MARKER_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_ALREADY_ANSWERED` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_ALREADY_RESOLVED` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_NOT_ANSWERED` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_NOT_ELIGIBLE` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_NOT_INVALIDATED` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_REPLY_ALREADY_USED` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_REPLY_AMBIGUOUS` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_REPLY_MISMATCH` | `src/workflow.mjs` |
| `WORKFLOW_THREAD_REPLY_NOT_OBSERVED` | `src/workflow.mjs` |
| `WORKFLOW_UNRESOLVE_ACTION_MISSING` | `src/publication.mjs` |
| `WORKFLOW_UNRESOLVE_RECORD_MISSING` | `src/workflow.mjs` |
| `WORKFLOW_UNRESOLVE_REFRESH_MISSING` | `src/workflow.mjs` |
| `WORKTREE_DIRTY` | `src/workflow.mjs` |
